# —— 运行主程序所需 ——
PySide6>=6.5.0
termqt>=1.0
Pillow>=10.0.0
PyYAML>=6.0
markdown>=3.5
openai>=1.40.0
pydantic>=2.0.0
typing_extensions>=4.8.0
langchain>=1.0.0
langchain-openai>=0.3.0
langgraph>=0.4.0
langgraph-checkpoint-sqlite>=2.0.0
pyperclip>=1.8.0
pypdf>=4.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
reportlab>=4.0.0
# 代码结构解析；本地可编辑安装：pip install -e ./tree-sitter-analyzer-main
tree-sitter-analyzer>=1.29.0
# 外部 MCP（未装则扩展面板提示安装）
langchain-mcp-adapters>=0.1.0
mcp>=1.0.0

# —— 打包（仅 build_linux.py / build_exe.py）——
# pyinstaller>=6.0.0

# —— 皮肤素材管线脚本（generate_q_assets / video_to_walk 等，非运行时必需）——
# rembg
# opencv-python-headless
# numpy
